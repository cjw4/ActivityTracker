<nav class="navbar navbar-default navbar-fixed-top">
	<!-- Brand and toggle get grouped for better mobile display -->
	<div class="container">
    <div class="navbar-header">
        <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a href="#" class="navbar-brand">Creature of Habit</a>
    </div>
    <!-- Collection of nav links and other content for toggling -->
		<div id="navbarCollapse" class="collapse navbar-collapse">
			<% if logged_in? %>
				<% unless @activity.nil? %>
					<ul class="nav navbar-nav">
						<li class="dropdown">
							<a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= @activity.name %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "New Entry", new_activity_entry_path(@activity) %></li>
							<li><%= link_to "Edit", edit_activity_path(@activity) %></li>
							<li><%= link_to "Download Data", { format: :csv } %></li>
              <li><%= link_to "Delete", activity_path(@activity), method: :delete %></li>
            </ul>
						</li>
					</ul>
				<% end %>
				<ul class="nav navbar-nav navbar-right">
					<li><%= link_to "Habits", current_user %></li>
					<li><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
					<li><%= link_to "Log Out", logout_path, method: :delete %></li>
				</ul>
			<% else %>
        <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "Home", root_path %></li>
            <li><%= link_to "Log In", login_path %></li>
        </ul>
			<% end %>
		</div>
	</div>
</nav>
